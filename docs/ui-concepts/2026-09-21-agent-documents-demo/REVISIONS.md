# Hiệu chỉnh sau khi xem ảnh

Các hiệu chỉnh này chỉ áp dụng cho ảnh đề xuất, không thay đổi baseline được duyệt.

## 03-agent-narrow

Edit this existing H2 Notes narrow-window UI mockup with only a tiny text correction. Preserve exact composition, aspect ratio, all content, colors, controls and fonts. In the progress card, remove any invented completion count or remaining-time estimate. Replace the subtitle '2/3 bước hoàn thành · Ước tính còn 1 phút' with 'Đang kiểm tra công thức'. Change any running label containing 'ước tính 1 phút' to simply 'Đang xử lý'. The three stages must remain: read workbook completed; checking formulas CURRENT; readback pending. No time estimate or completed percentage anywhere. Keep the one-row composer and stop control intact. Sharp readable Vietnamese. Do not change anything else.

## 16-assistant-380

Edit this H2 Assistant small floating window mockup. Keep the exact portrait 380:610 aspect ratio, warm cream terracotta visual style, readable Vietnamese, central chat, progress card with D51 completed and D52 still checking, and fixed composer with exactly one action row plus/shield/Ollama/microphone/stop. Correct the TOP HEADER ONLY: this is a GLOBAL floating Assistant, with no attached project. Replace the redundant two-level title bars and project details with ONE compact top title bar H2 logo + 'Assistant' + pin + expand + close. Completely remove 'RPBMVN Thạch Bích', '3/5 công việc', and 'Tiếp theo: Hoàn thiện dự toán'. Immediately below header show scope 'Máy tính này' and chips Excel and DuToan.xlsx. Do not show a project name anywhere. Reclaim removed header space for conversational breathing room, maintain real 380DIP legibility. Preserve remaining chat text and running state. Footer 'Demo · 380 × 610 DIP'.

## 17-assistant-full

Perform two precisely scoped changes to this existing full H2 Assistant desktop mockup and preserve all remaining pixels/layout as closely as possible. 1) In the LEFT app navigation select 'Trợ lý': its icon and text terracotta on a pale peach selection background with the left terracotta indicator. 'Ghi chú' must be neutral/unselected. 2) Under center header 'H2 Assistant', make the scope explicit with readable text 'Máy tính này · Chưa gắn dự án' and retain 'Gắn vào dự án' button. No project is attached. Preserve the verified result, file evidence, three column layout, warm cream colors, titlebar, and one-row composer. No new UI or other changes.

