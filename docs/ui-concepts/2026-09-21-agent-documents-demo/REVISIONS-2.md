# Hiệu chỉnh trạng thái dữ liệu minh họa

## 07-command-center

Make small precise state-label corrections to this H2 Notes Command Center mockup. Keep the exact six-card composition, warm ivory/terracotta design, all project progress fractions and other content. On Thạch Bích project card set Agent state to orange 'Cần bạn duyệt' because the top attention strip requests permission to fix two formulas. Keep its separate sync state green 'Đã đồng bộ'. On Cầu Vạn project card set Agent state neutral 'Sẵn sàng'; set its SEPARATE sync state orange 'Xung đột ghi chú' instead of green synced because the attention strip asks to resolve note sync conflict. Never conflate sync errors with Agent state. On the completed Hồ sơ nghiệm thu card with 5/5 tasks replace the next-task phrase with 'Chưa có việc tiếp theo', and Agent neutral 'Sẵn sàng'. Keep controls, geometry, typography and project names exactly. All data illustrative.

## 19-storage-transfer

Edit this H2 Notes storage-transfer UI mockup with these precise corrections and preserve warm cream/terracotta style and main local-folder comparison, three explicit choices, empty radio circles, backup note and disabled preview button. 1) NONE of the three choices is selected or hovered: change Ghi đè card from orange highlighted border/background/icon to the SAME neutral white background, gray thin border and charcoal icon as the other cards. Keep all three radio circles empty. 2) Subtitle below 'Chuyển thư mục lưu cục bộ' must be 'Chọn cách xử lý dữ liệu trước khi chuyển' instead of wording which assumes merge. 3) Remove the unrelated project header 'RPBMVN Thạch Bích', project progress, project tabs Agent/Công việc/Ghi chú/Tệp/Lịch sử and project/file chips from top of content area. This is a GLOBAL settings page. Use clean content header 'Cài đặt / Dữ liệu và đồng bộ' there; compact space to fit remaining content beautifully. Preserve source D:\H2Data and target E:\H2Archive, safeguard text, Hủy and disabled 'Xem trước thay đổi'. Do not add new actions.

## 20-pdf-evidence

Perform one tiny Vietnamese wording correction to this existing H2 Notes Agent plus PDF viewer mockup. The comparison table shows two compared items, only ONE discrepancy: Ván khuôn 210 vs215, while Lắp đặt180vs180 matches. Therefore replace the Agent sentence 'Có 2 hạng mục cần rà lại. Xem phần được đánh dấu trong phụ lục.' with 'Đã đối chiếu 2 hạng mục; 1 hạng mục chênh lệch. Xem phần được đánh dấu trong phụ lục.' Keep ALL other composition, design, labels, source reference, tables, highlighting, read-only footer and controls unchanged. Sharp legible Vietnamese.

