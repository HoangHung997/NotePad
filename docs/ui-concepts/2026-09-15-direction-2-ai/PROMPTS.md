# H2 Notes direction 2: AI layout prompts

Mode: built-in image_gen. Reference image (style only): D:/VSstudio/Nodepad/docs/ui-concepts/2026-09-15/02a-tap-trung-du-an.png

## 2.1

Use case: ui-mockup. Create ONE high-fidelity desktop UI concept image, not an implemented screenshot. H2 Notes is a lightweight C# personal project/checklist/rich-note application. The attached image is a STYLE and MASTER-DETAIL reference only. Preserve its warm ivory paper (#F7F3EC), charcoal (#302E2B), restrained terracotta (#A4573D), crisp thin rules, small near-square corners, H2 square monogram, elegant but compact typography. Completely replace its RIGHT WORKSPACE with the layout specified below. Do NOT preserve the old giant project heading: new title must be a compact 22px-equivalent line to give space to work. All five variants share the same brand, colors and LEFT project navigation; compare layout, not recoloring.
Output full window front-on, orthographic, landscape approximately 1900x1200, small neutral exterior margin. No photographed screen, perspective or blur. Native desktop feel, not marketing dashboard. Extremely sharp readable Vietnamese text and diacritics, sensible compact desktop density. No charts, KPI cards, calendars, decorative avatars, formulas or Excel ribbon. No taskbar, no Windows minimize or maximize buttons. App top bar only H2 Notes brand and pin, three-dot menu, X. Pin and X remain clear.
COMMON LEFT SIDE (roughly 23% total): narrow icon rail for Dự án, Ghi chú, AI and Cài đặt; beside it a Zalo-like PROJECT LIST, not people/contacts. Heading "Dự án" with small plus; search "Tìm dự án"; rows with small numbered square project icons, project name, and short muted next-action preview, showing "Đường Gom CT_TA_171" / 1/3, "Đường tỉnh 156 - Lào Cai" / 0/2, selected "Điện Hạt Nhân I - Ninh Thuận" / 2/3 on soft peach background, "Đường gom QL18" / 1/4, "Cầu Vạn - Kinh Môn" / 2/5. Names wrap cleanly. Avoid people, phone buttons or user contact avatars. Left bottom tiny archive action. Selected project is a workspace with its OWN AI conversation history.
COMMON PROJECT DATA when shown: selected project has 3 tasks: checked "Chia lại khối lượng" with remark "Đã gửi 09/06"; checked "Dựng HSHC" with remark "Đã xong"; unchecked "Gửi hồ sơ bổ sung" with remark "Chờ duyệt". Progress 2/3. Next action is "Gửi hồ sơ bổ sung". Project notes are "Tạm hoàn thành HSHC, chờ chủ đầu tư chốt." and "Cập nhật khối lượng khi có xác nhận mới." Rich note controls B I U A and font selector appear only in relevant note area.
COMMON AI UX: a real visible conversational thread, not only an AI icon. A clearly readable segmented selector "Trên máy" and "API online" plus small connection/settings icon. No real model/provider names or credentials needed. Context chips show which project's tasks/notes or selected passage AI may use; a small "Xem ngữ cảnh" or "Xem dữ liệu gửi" action. A message composer and send arrow must be visible. Suggestions from AI are drafts and use explicit action buttons "Thêm công việc" and/or "Lưu vào ghi chú", never auto-change project data. AI should not mark tasks done by itself. Short plausible sample exchange: user "Việc nào cần làm tiếp?"; AI "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." The reply can include proposed "Kiểm tra phụ lục" and an action to add it. For local mode small state "Xử lý trên máy"; for online mode small disclosure "Chỉ gửi ngữ cảnh đã chọn". Avoid intrusive warning dialogs. Keep prose short for legibility. This is a design concept only.

VARIANT 2.1: "BA VÙNG SONG SONG". This screen uses a THREE-COLUMN composition: project list LEFT 23%, task-and-notes CENTER 45%, permanent AI conversation RIGHT 32%. Preserve warm ivory/terracotta reference look but make header compact. A thin vertical drag splitter with small grip divides center from AI. No bottom chat dock, no floating window.
CENTER header compact selected name and priority star, progress 2/3 on same row. Under it a compact task table with all three tasks and their remarks, plus "+ Công việc" text action. Below a thin collapsible "Ghi chú dự án" header and rich toolbar. The notes editor fills remaining center height with the two sample note paragraphs and visible caret. No giant title or duplicated summary boxes.
RIGHT AI header "Trợ lý dự án" plus collapse arrow. Directly underneath show selector "Trên máy" SELECTED and "API online" unselected, small "Mô hình cục bộ ▾" row and settings gear. Context strip "Dự án này" and removable chips "3 công việc ×" "Ghi chú ×", small "Xem ngữ cảnh".
Chat user right bubble "Việc nào cần làm tiếp?". AI left reply with small H2 glyph: "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." Beneath response a small outlined suggestion "Kiểm tra phụ lục" with "+ Thêm công việc" button, and a quieter "Lưu vào ghi chú" link. Clearly proposed, not already added. Bottom anchored composer "Hỏi AI về dự án này…" with paperclip and send arrow, tiny footer "Xử lý trên máy".
Keep all text comfortably readable and all three zones visually balanced. Subtle external caption "2.1  BA VÙNG SONG SONG". No explanatory diagrams outside screenshot.

## 2.2

Use case: ui-mockup. Create ONE high-fidelity desktop UI concept image, not an implemented screenshot. H2 Notes is a lightweight C# personal project/checklist/rich-note application. The attached image is a STYLE and MASTER-DETAIL reference only. Preserve its warm ivory paper (#F7F3EC), charcoal (#302E2B), restrained terracotta (#A4573D), crisp thin rules, small near-square corners, H2 square monogram, elegant but compact typography. Completely replace its RIGHT WORKSPACE with the layout specified below. Do NOT preserve the old giant project heading: new title must be a compact 22px-equivalent line to give space to work. All five variants share the same brand, colors and LEFT project navigation; compare layout, not recoloring.
Output full window front-on, orthographic, landscape approximately 1900x1200, small neutral exterior margin. No photographed screen, perspective or blur. Native desktop feel, not marketing dashboard. Extremely sharp readable Vietnamese text and diacritics, sensible compact desktop density. No charts, KPI cards, calendars, decorative avatars, formulas or Excel ribbon. No taskbar, no Windows minimize or maximize buttons. App top bar only H2 Notes brand and pin, three-dot menu, X. Pin and X remain clear.
COMMON LEFT SIDE (roughly 23% total): narrow icon rail for Dự án, Ghi chú, AI and Cài đặt; beside it a Zalo-like PROJECT LIST, not people/contacts. Heading "Dự án" with small plus; search "Tìm dự án"; rows with small numbered square project icons, project name, and short muted next-action preview, showing "Đường Gom CT_TA_171" / 1/3, "Đường tỉnh 156 - Lào Cai" / 0/2, selected "Điện Hạt Nhân I - Ninh Thuận" / 2/3 on soft peach background, "Đường gom QL18" / 1/4, "Cầu Vạn - Kinh Môn" / 2/5. Names wrap cleanly. Avoid people, phone buttons or user contact avatars. Left bottom tiny archive action. Selected project is a workspace with its OWN AI conversation history.
COMMON PROJECT DATA when shown: selected project has 3 tasks: checked "Chia lại khối lượng" with remark "Đã gửi 09/06"; checked "Dựng HSHC" with remark "Đã xong"; unchecked "Gửi hồ sơ bổ sung" with remark "Chờ duyệt". Progress 2/3. Next action is "Gửi hồ sơ bổ sung". Project notes are "Tạm hoàn thành HSHC, chờ chủ đầu tư chốt." and "Cập nhật khối lượng khi có xác nhận mới." Rich note controls B I U A and font selector appear only in relevant note area.
COMMON AI UX: a real visible conversational thread, not only an AI icon. A clearly readable segmented selector "Trên máy" and "API online" plus small connection/settings icon. No real model/provider names or credentials needed. Context chips show which project's tasks/notes or selected passage AI may use; a small "Xem ngữ cảnh" or "Xem dữ liệu gửi" action. A message composer and send arrow must be visible. Suggestions from AI are drafts and use explicit action buttons "Thêm công việc" and/or "Lưu vào ghi chú", never auto-change project data. AI should not mark tasks done by itself. Short plausible sample exchange: user "Việc nào cần làm tiếp?"; AI "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." The reply can include proposed "Kiểm tra phụ lục" and an action to add it. For local mode small state "Xử lý trên máy"; for online mode small disclosure "Chỉ gửi ngữ cảnh đã chọn". Avoid intrusive warning dialogs. Keep prose short for legibility. This is a design concept only.

VARIANT 2.2: "TRÒ CHUYỆN LÀ TRUNG TÂM". This is the layout most like a Zalo conversation, while retaining H2 Notes ivory/terracotta branding, NOT a Zalo clone/logo. TWO main columns only: common project list 23% left; one wide conversation 77% right. NO permanent right inspector, no split bottom dock, no floating chat.
On right, compact conversation header "Điện Hạt Nhân I - Ninh Thuận" and smaller "Trợ lý riêng của dự án" with thread/history icon and contextual menu. Right side of this header shows segmented "Trên máy" unselected / "API online" SELECTED, tiny settings cog.
Immediately under header is a COMPACT PINNED PROJECT AREA, at most 22% of total right height: title row "Đã ghim" with pin icon and "Thu gọn"; show two short side-by-side snippets within it. Left snippet "Công việc 2/3" and the three tight checkbox task lines; right snippet "Ghi chú dự án" with two short note lines and "Mở ghi chú". This area stays small enough that CHAT dominates at least 60% of right workspace height.
Main chat timeline below on warm off-white: thin separator "Hôm nay"; user right pale-peach bubble "Tóm tắt tình hình và cho tôi biết việc tiếp theo."; assistant left response, no oversized portrait:
"Đã hoàn thành 2/3 công việc.
Còn: Gửi hồ sơ bổ sung.
Lưu ý: Chờ chủ đầu tư xác nhận khối lượng."
A second compact assistant suggestion message contains outlined card "Đề xuất công việc: Kiểm tra phụ lục trước khi gửi hồ sơ" with button "+ Thêm vào checklist" and secondary "Lưu vào ghi chú". Small source chips under assistant reply "Checklist" "Ghi chú dự án" to identify context. Conversation readable like messaging, bubbles not sprawling giant cards.
Bottom fixed composer across entire right width. Immediately above text input, selected context chips "Dự án này ×" "3 công việc ×" "Ghi chú ×", and small "Xem dữ liệu gửi". Input placeholder "Nhắn với AI về dự án này…" with paperclip, send arrow. Tiny footer "API online · Chỉ gửi ngữ cảnh đã chọn". All messages illustrative, not real credentials. Caption outside app "2.2  TRÒ CHUYỆN LÀ TRUNG TÂM".

## 2.3

Use case: ui-mockup. Create ONE high-fidelity desktop UI concept image, not an implemented screenshot. H2 Notes is a lightweight C# personal project/checklist/rich-note application. The attached image is a STYLE and MASTER-DETAIL reference only. Preserve its warm ivory paper (#F7F3EC), charcoal (#302E2B), restrained terracotta (#A4573D), crisp thin rules, small near-square corners, H2 square monogram, elegant but compact typography. Completely replace its RIGHT WORKSPACE with the layout specified below. Do NOT preserve the old giant project heading: new title must be a compact 22px-equivalent line to give space to work. All five variants share the same brand, colors and LEFT project navigation; compare layout, not recoloring.
Output full window front-on, orthographic, landscape approximately 1900x1200, small neutral exterior margin. No photographed screen, perspective or blur. Native desktop feel, not marketing dashboard. Extremely sharp readable Vietnamese text and diacritics, sensible compact desktop density. No charts, KPI cards, calendars, decorative avatars, formulas or Excel ribbon. No taskbar, no Windows minimize or maximize buttons. App top bar only H2 Notes brand and pin, three-dot menu, X. Pin and X remain clear.
COMMON LEFT SIDE (roughly 23% total): narrow icon rail for Dự án, Ghi chú, AI and Cài đặt; beside it a Zalo-like PROJECT LIST, not people/contacts. Heading "Dự án" with small plus; search "Tìm dự án"; rows with small numbered square project icons, project name, and short muted next-action preview, showing "Đường Gom CT_TA_171" / 1/3, "Đường tỉnh 156 - Lào Cai" / 0/2, selected "Điện Hạt Nhân I - Ninh Thuận" / 2/3 on soft peach background, "Đường gom QL18" / 1/4, "Cầu Vạn - Kinh Môn" / 2/5. Names wrap cleanly. Avoid people, phone buttons or user contact avatars. Left bottom tiny archive action. Selected project is a workspace with its OWN AI conversation history.
COMMON PROJECT DATA when shown: selected project has 3 tasks: checked "Chia lại khối lượng" with remark "Đã gửi 09/06"; checked "Dựng HSHC" with remark "Đã xong"; unchecked "Gửi hồ sơ bổ sung" with remark "Chờ duyệt". Progress 2/3. Next action is "Gửi hồ sơ bổ sung". Project notes are "Tạm hoàn thành HSHC, chờ chủ đầu tư chốt." and "Cập nhật khối lượng khi có xác nhận mới." Rich note controls B I U A and font selector appear only in relevant note area.
COMMON AI UX: a real visible conversational thread, not only an AI icon. A clearly readable segmented selector "Trên máy" and "API online" plus small connection/settings icon. No real model/provider names or credentials needed. Context chips show which project's tasks/notes or selected passage AI may use; a small "Xem ngữ cảnh" or "Xem dữ liệu gửi" action. A message composer and send arrow must be visible. Suggestions from AI are drafts and use explicit action buttons "Thêm công việc" and/or "Lưu vào ghi chú", never auto-change project data. AI should not mark tasks done by itself. Short plausible sample exchange: user "Việc nào cần làm tiếp?"; AI "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." The reply can include proposed "Kiểm tra phụ lục" and an action to add it. For local mode small state "Xử lý trên máy"; for online mode small disclosure "Chỉ gửi ngữ cảnh đã chọn". Avoid intrusive warning dialogs. Keep prose short for legibility. This is a design concept only.

VARIANT 2.3: "AI Ở NGĂN DƯỚI". TWO main columns: common project list LEFT 23%; the RIGHT 77% is divided HORIZONTALLY into upper project workspace 55% height and lower AI chat dock 45%. Absolutely NO full-height right AI column and no floating popup. The horizontal splitter is unmistakable: thin line and centered grab handle; small up/down resize cursor over grip suggests it can be resized.
Right TOP header compact selected project name, star, and "+ Công việc" at far right. Below, tabs "Công việc" active and "Ghi chú". Task table spans the whole right width with three checkbox rows and remarks. Under table one thin "Ghi chú nhanh" inline section with small B I U A toolbar and the two sample note paragraphs, around 100px tall. Progress "2/3" appears in table section heading, no huge header or KPI.
Right BOTTOM dock starts with a narrow header "Trò chuyện AI" and thread history icon on left; segmented "Trên máy" SELECTED / "API online" unselected and cog aligned to right. Under header a narrow context row "Dự án này" "3 công việc ×" "Ghi chú ×", small "Xem ngữ cảnh".
Below show actual conversational messages in the lower dock: right-aligned user bubble "Việc nào cần làm tiếp?"; left-aligned assistant block "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." Under reply two small inline actions "+ Thêm công việc" and "Lưu vào ghi chú". Keep these as suggestions, not automatically completed tasks. Composer spans width along the bottom with placeholder "Hỏi AI về dự án này…" and send arrow, local status "Xử lý trên máy". 
All right area maintains full width, making long task names and long notes readable without narrow-column wrapping. Balanced density with chat contents not squashed. Slim native control styling, readable sans body and modest serif headings. Caption outside app "2.3  AI Ở NGĂN DƯỚI".

## 2.4

Use case: ui-mockup. Create ONE high-fidelity desktop UI concept image, not an implemented screenshot. H2 Notes is a lightweight C# personal project/checklist/rich-note application. The attached image is a STYLE and MASTER-DETAIL reference only. Preserve its warm ivory paper (#F7F3EC), charcoal (#302E2B), restrained terracotta (#A4573D), crisp thin rules, small near-square corners, H2 square monogram, elegant but compact typography. Completely replace its RIGHT WORKSPACE with the layout specified below. Do NOT preserve the old giant project heading: new title must be a compact 22px-equivalent line to give space to work. All five variants share the same brand, colors and LEFT project navigation; compare layout, not recoloring.
Output full window front-on, orthographic, landscape approximately 1900x1200, small neutral exterior margin. No photographed screen, perspective or blur. Native desktop feel, not marketing dashboard. Extremely sharp readable Vietnamese text and diacritics, sensible compact desktop density. No charts, KPI cards, calendars, decorative avatars, formulas or Excel ribbon. No taskbar, no Windows minimize or maximize buttons. App top bar only H2 Notes brand and pin, three-dot menu, X. Pin and X remain clear.
COMMON LEFT SIDE (roughly 23% total): narrow icon rail for Dự án, Ghi chú, AI and Cài đặt; beside it a Zalo-like PROJECT LIST, not people/contacts. Heading "Dự án" with small plus; search "Tìm dự án"; rows with small numbered square project icons, project name, and short muted next-action preview, showing "Đường Gom CT_TA_171" / 1/3, "Đường tỉnh 156 - Lào Cai" / 0/2, selected "Điện Hạt Nhân I - Ninh Thuận" / 2/3 on soft peach background, "Đường gom QL18" / 1/4, "Cầu Vạn - Kinh Môn" / 2/5. Names wrap cleanly. Avoid people, phone buttons or user contact avatars. Left bottom tiny archive action. Selected project is a workspace with its OWN AI conversation history.
COMMON PROJECT DATA when shown: selected project has 3 tasks: checked "Chia lại khối lượng" with remark "Đã gửi 09/06"; checked "Dựng HSHC" with remark "Đã xong"; unchecked "Gửi hồ sơ bổ sung" with remark "Chờ duyệt". Progress 2/3. Next action is "Gửi hồ sơ bổ sung". Project notes are "Tạm hoàn thành HSHC, chờ chủ đầu tư chốt." and "Cập nhật khối lượng khi có xác nhận mới." Rich note controls B I U A and font selector appear only in relevant note area.
COMMON AI UX: a real visible conversational thread, not only an AI icon. A clearly readable segmented selector "Trên máy" and "API online" plus small connection/settings icon. No real model/provider names or credentials needed. Context chips show which project's tasks/notes or selected passage AI may use; a small "Xem ngữ cảnh" or "Xem dữ liệu gửi" action. A message composer and send arrow must be visible. Suggestions from AI are drafts and use explicit action buttons "Thêm công việc" and/or "Lưu vào ghi chú", never auto-change project data. AI should not mark tasks done by itself. Short plausible sample exchange: user "Việc nào cần làm tiếp?"; AI "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." The reply can include proposed "Kiểm tra phụ lục" and an action to add it. For local mode small state "Xử lý trên máy"; for online mode small disclosure "Chỉ gửi ngữ cảnh đã chọn". Avoid intrusive warning dialogs. Keep prose short for legibility. This is a design concept only.

VARIANT 2.4: "AI BẬT KHI CẦN". Common LEFT project list stays 23%. RIGHT 77% is a spacious SINGLE DOCUMENT EDITOR, with NO fixed AI split pane and NO chat dock. AI appears as a clearly FLOATING NON-MODAL compact chat tool window over the LOWER RIGHT of the writing area, with a restrained shadow showing it can move. This must not look like a third full-height column.
RIGHT workspace header: compact selected project name, priority star, small "2/3 công việc". Below tabs "Công việc" and "Ghi chú" SELECTED, plus small outlined button "Hỏi AI" near right. The writing editor has compact Cambria 14 B I U A toolbar, then text with a modest serif heading "Ghi chú dự án", the two sample note paragraphs. Select ONLY the sentence "Cập nhật khối lượng khi có xác nhận mới." with pale peach highlight. Below are two extra modest headings "Cần lưu ý" and "Kết luận" with short lines "Kiểm tra phụ lục trước khi gửi." and "Chờ xác nhận từ chủ đầu tư.". Keep selected sentence and main text visible to the LEFT/ABOVE floating AI. Top or bottom a small collapsed task strip "Công việc 2/3 · Gửi hồ sơ bổ sung" can be opened, but no task table competing with note document.
FLOATING AI rectangle ~400x440px-equivalent on lower right, enough room for legible short conversation but not blocking the selected paragraph. Title strip "Hỏi AI" with six-dot drag handle, a dock icon "Ghim vào cạnh" as tooltip hint, and small X closes ONLY this AI popup. Under strip show segmented "Trên máy" / "API online" SELECTED. Important scope line "Chỉ đoạn đang chọn" with small "Xem dữ liệu gửi". Do NOT automatically send all project notes.
User bubble "Viết gọn đoạn này giúp tôi."; AI reply "Cập nhật khối lượng sau khi được xác nhận.". Under this preview two explicit actions "Thay đoạn đã chọn" and "Chèn bên dưới". No automatic editing. Composer "Hỏi tiếp…" and send arrow below. Tiny privacy caption "API online · Chỉ gửi đoạn đã chọn".
A cursor near popup drag handle may indicate movability. Outer app stays fully visible. Normal note footer "Đã lưu" at bottom left. Avoid generic modal backdrop dimming: editor remains usable around popup. External caption "2.4  AI BẬT KHI CẦN".

## 2.5

Use case: ui-mockup. Create ONE high-fidelity desktop UI concept image, not an implemented screenshot. H2 Notes is a lightweight C# personal project/checklist/rich-note application. The attached image is a STYLE and MASTER-DETAIL reference only. Preserve its warm ivory paper (#F7F3EC), charcoal (#302E2B), restrained terracotta (#A4573D), crisp thin rules, small near-square corners, H2 square monogram, elegant but compact typography. Completely replace its RIGHT WORKSPACE with the layout specified below. Do NOT preserve the old giant project heading: new title must be a compact 22px-equivalent line to give space to work. All five variants share the same brand, colors and LEFT project navigation; compare layout, not recoloring.
Output full window front-on, orthographic, landscape approximately 1900x1200, small neutral exterior margin. No photographed screen, perspective or blur. Native desktop feel, not marketing dashboard. Extremely sharp readable Vietnamese text and diacritics, sensible compact desktop density. No charts, KPI cards, calendars, decorative avatars, formulas or Excel ribbon. No taskbar, no Windows minimize or maximize buttons. App top bar only H2 Notes brand and pin, three-dot menu, X. Pin and X remain clear.
COMMON LEFT SIDE (roughly 23% total): narrow icon rail for Dự án, Ghi chú, AI and Cài đặt; beside it a Zalo-like PROJECT LIST, not people/contacts. Heading "Dự án" with small plus; search "Tìm dự án"; rows with small numbered square project icons, project name, and short muted next-action preview, showing "Đường Gom CT_TA_171" / 1/3, "Đường tỉnh 156 - Lào Cai" / 0/2, selected "Điện Hạt Nhân I - Ninh Thuận" / 2/3 on soft peach background, "Đường gom QL18" / 1/4, "Cầu Vạn - Kinh Môn" / 2/5. Names wrap cleanly. Avoid people, phone buttons or user contact avatars. Left bottom tiny archive action. Selected project is a workspace with its OWN AI conversation history.
COMMON PROJECT DATA when shown: selected project has 3 tasks: checked "Chia lại khối lượng" with remark "Đã gửi 09/06"; checked "Dựng HSHC" with remark "Đã xong"; unchecked "Gửi hồ sơ bổ sung" with remark "Chờ duyệt". Progress 2/3. Next action is "Gửi hồ sơ bổ sung". Project notes are "Tạm hoàn thành HSHC, chờ chủ đầu tư chốt." and "Cập nhật khối lượng khi có xác nhận mới." Rich note controls B I U A and font selector appear only in relevant note area.
COMMON AI UX: a real visible conversational thread, not only an AI icon. A clearly readable segmented selector "Trên máy" and "API online" plus small connection/settings icon. No real model/provider names or credentials needed. Context chips show which project's tasks/notes or selected passage AI may use; a small "Xem ngữ cảnh" or "Xem dữ liệu gửi" action. A message composer and send arrow must be visible. Suggestions from AI are drafts and use explicit action buttons "Thêm công việc" and/or "Lưu vào ghi chú", never auto-change project data. AI should not mark tasks done by itself. Short plausible sample exchange: user "Việc nào cần làm tiếp?"; AI "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." The reply can include proposed "Kiểm tra phụ lục" and an action to add it. For local mode small state "Xử lý trên máy"; for online mode small disclosure "Chỉ gửi ngữ cảnh đã chọn". Avoid intrusive warning dialogs. Keep prose short for legibility. This is a design concept only.

VARIANT 2.5: "KHÔNG GIAN TỰ SẮP XẾP". Keep common left project list at 23%; right 77% is a DOCKABLE THREE-PANEL WORKSPACE, materially different from variants above. Display this layout: TOP full-width AI chat panel occupying 48% of right height, BELOW it two equal-width panels SIDE BY SIDE, CHECKLIST lower left and NOTES lower right occupying 52% height. Clear thin horizontal and vertical resize splitters. Not a permanent full-height right AI column, not chat bottom, not floating AI.
Compact workspace topbar selected project name "Điện Hạt Nhân I - Ninh Thuận", priority star, and right dropdown "Bố cục: Tùy chỉnh ▾". A tiny saved-layout indication. Panel headers have SIX-DOT DRAG HANDLES, title and simple collapse arrow; a pointer is hovering the notes handle with small tooltip "Kéo để đổi vị trí". This indicates panels can rearrange. Keep header labels readable; do not draw huge arrows or duplicate ghost windows.
TOP PANEL titled "Trò chuyện AI". Header right has segmented selector "Trên máy" SELECTED / "API online" and gear. Thin context strip "Dự án này ×" "3 công việc ×" "Ghi chú ×" "Xem ngữ cảnh". Compact conversation inside: user right bubble "Việc nào cần làm tiếp?"; AI left response "Còn việc gửi hồ sơ bổ sung. Nên kiểm tra phụ lục trước khi gửi." with two inline buttons "+ Thêm công việc" and "Lưu vào ghi chú". Composer along bottom edge of TOP panel, NOT bottom of app: "Hỏi AI về dự án này…" and send arrow. Small state "Xử lý trên máy". Show a real usable conversation, not only the header.
BOTTOM LEFT PANEL titled "Công việc" with badge "2/3" and small plus. Compact vertical checkbox table with the three exact tasks and brief remarks, unchecked final item. Bottom of this panel has subtle next-action line "Tiếp: Gửi hồ sơ bổ sung" and "+ Công việc".
BOTTOM RIGHT PANEL titled "Ghi chú", a small formatting toolbar "Cambria 14 B I U A"; paper editor containing both sample note paragraphs, bold miniheading "Cần lưu ý" and line "Kiểm tra phụ lục.", visible caret. Tiny "Đã lưu" lower right.
All three panels are FLAT separated workspace panes, not decorative rounded dashboard cards. The layout feels like light configurable native productivity software, not an IDE full of code or a web analytics dashboard. Warm ivory, charcoal and terracotta as reference. Enough room for reading and editing. Thin statusbar "Bố cục đã lưu cho dự án này". External caption "2.5  KHÔNG GIAN TỰ SẮP XẾP".

